.. _convert:

Convert La Palma Data Cubes to HDF5 Files
=========================================

The following will demonstrate how to use `crispy` to convert a La Palma legacy data cube into a directory of HDF5 files (soon to be changed to zarr) where each file is a specific timestep.